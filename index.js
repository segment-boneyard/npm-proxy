
/**
 * Module dependencies.
 */

var assert = require('assert');

/**
 * Expose `Proxy`.
 */

module.exports = Proxy;

/**
 * Initialize a proxy.
 *
 * @param {Object} opts
 * @api public
 */

function Proxy(opts) {
  assert(opts, 'settings required');
}